# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this directory.

## Scope

- Pipeline contracts, validation rules, registry, and shared types.
- Used by `services/pipelines/*` to bind specs to implementations.
