# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this directory.

## Scope

- Centralized configuration schemas and loader.
- Config file lives at `config/app.yaml` (or `APP_CONFIG_PATH`) with env overrides.
