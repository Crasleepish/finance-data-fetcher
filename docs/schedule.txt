## 0) 基础工程与质量门禁（已完成）

## 1) 数据库访问层（infra/db）（prefer Core）（已完成）

## 2) 任务状态与幂等基础设施（infra/task_state + infra/idempotency）（已完成）

## 3) 交易日管理模块（core/calendar + infra/calendar_store）（已完成）

## 4) 异步执行底座（infra/queue + worker runtime）（已完成）

## 5) Pipeline 核心抽象（先定接口，再写实现）（已完成）

## 6) ChunkPolicy：Arguments → chunks_of_arguments（可复用策略模板）

## 7) Fetcher（chunk 级 retry ONLY）

## 8) Cleaner（与 Pipeline 绑定）

## 9) WorkflowEngine（pipeline-aware + chunk loop + progress）

## 10) PipelineFailoverPolicy（可选但位置固定）

## 11) API 层（控制面）

## 12) 测试与稳定性补齐
